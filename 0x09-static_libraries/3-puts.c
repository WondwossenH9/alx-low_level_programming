#include "main.h"
#include <stdio.h>
/**
 * _puts- prototype function
 * @str: string pointer
 */
void _puts(char *str);
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("Hello, World!");

	return (0);
}
