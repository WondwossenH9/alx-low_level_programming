#include "main.h"
/**
 * add - adds two integers and returns the result
 * @a: first paramenter to be added
 * @b: second parameter to be added
 * Return: integer value sum
 */
int add(int a, int b)
{
	return (a + b);
}
